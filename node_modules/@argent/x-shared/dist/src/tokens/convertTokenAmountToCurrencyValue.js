import { DEFAULT_TOKEN_DECIMALS as i } from "./constants.js";
import { isNumeric as r } from "../utils/number/isNumeric.js";
import { parseCurrency as c } from "../bigdecimal/utils.js";
import { formatUnits as u } from "../bigdecimal/formatUnits.js";
const l = ({ amount: o, decimals: e, unitCurrencyValue: t }) => {
  if (!r(o) || !r(e) || !r(t))
    return;
  const m = Number(e), n = BigInt(o) * c(t.toString()).value;
  return u({
    value: n,
    decimals: m + i
  });
};
export {
  l as convertTokenAmountToCurrencyValue
};
