function s({ value: t, decimals: n }) {
  if (n === 0)
    return t.toString();
  const r = t < 0n;
  t = r ? -t : t;
  const o = t.toString().padStart(n, "0"), i = o.slice(0, -n) || "0", c = o.slice(-n).replace(/0+$/, "");
  return `${r ? "-" : ""}${i}${c ? `.${c}` : ""}`;
}
export {
  s as formatUnits
};
