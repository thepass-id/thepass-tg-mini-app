import { constants as e } from "starknet";
import { NetworkError as a } from "../../errors/network.js";
function o(t) {
  switch (t) {
    case "mainnet-alpha":
    case e.NetworkName.SN_MAIN:
      return e.StarknetChainId.SN_MAIN;
    case "sepolia-alpha":
    case e.NetworkName.SN_SEPOLIA:
      return e.StarknetChainId.SN_SEPOLIA;
    default:
      throw new a({
        code: "NOT_FOUND",
        message: `Unknown networkId: ${t}`
      });
  }
}
export {
  o as getChainIdFromNetworkId
};
