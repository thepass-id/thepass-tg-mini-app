import { z as n } from "zod";
const a = n.string().regex(/^([a-zA-Z0-9-]+\.)+stark$/, "Invalid Starknet ID").max(50, "Starknet ID cannot be over 50 characters"), c = (r) => a.safeParse(r).success, o = (r, t) => {
  try {
    return t ? e(r) === e(t) : !1;
  } catch {
  }
  return !1;
}, e = (r) => a.parse(r).toLowerCase();
export {
  o as isEqualStarknetId,
  c as isStarknetId,
  e as normalizeStarknetId,
  a as starknetIdSchema
};
