import { isString as c, isEmpty as o } from "lodash-es";
const e = /(0x[a-fA-F0-9]+)\s*\('(.*?)'\)/, i = (r) => {
  if (!c(r))
    return;
  const t = r.match(e);
  if (t && !o(t[2]))
    return t[2];
};
export {
  i as getPrettyRpcError
};
