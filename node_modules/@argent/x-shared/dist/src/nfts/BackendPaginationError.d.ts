declare abstract class CustomError<T extends string | number> extends Error {
    name: string;
    code: T;
    constructor(name: string, code: T);
}
/** @deprecated use the new error classes instead */
export declare class BackendPaginationError extends CustomError<"tooLow" | "notANumber"> {
    constructor(code: "tooLow" | "notANumber");
}
export {};
